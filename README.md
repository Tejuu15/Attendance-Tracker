# Attendance Tracker — simple Flask UI

Run the web UI locally:

```bash
python -m pip install -r requirements.txt
python app.py
```

Open http://127.0.0.1:5000/ in your browser.

The original CLI is in `f.py` and is now import-safe (guarded by `if __name__ == "__main__"`).
